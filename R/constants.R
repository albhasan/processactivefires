# Name of the variables in the NetCDF when its resolution is 375 meters.
VAR.NAMES.375 <- c(
  "FP_longitude", "FP_latitude", "fire mask",
  "FP_confidence", "FP_power", "FP_line", "FP_sample"
)

# Name of the variables in the NetCDF when its resolution is 750 meters.
VAR.NAMES.750 <- c(
  "FP_longitude", "FP_latitude", "fire mask",
  "FP_confidence", "FP_T13", "FP_power", "FP_line", "FP_sample"
)

# Codes that identify fire in the data in the NetCDF.
FIRE.CODES <- 7:9
